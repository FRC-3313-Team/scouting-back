<!DOCTYPE html>
<html>
  <head>
    <% include ../partials/head %>

    <script src="/js/login.js" defer></script>

    <title>FRC Scout | Login</title>
  </head>
  <body>
    <div class="columns" id="app">
      <div class="column is-one-quarter">
        <section class="section">
          <h1 class="title">Login</h1>
          <p class="subtitle">FRC Scout 2019</p>
          <hr />

          <!-- form starts here -->
          <section class="form">
            <form v-on:submit.prevent="login">
              <div class="field">
                <div class="control">
                  <input
                    class="input"
                    type="password"
                    placeholder="Password"
                    v-model="password"
                    v-bind:class="{ 'is-danger': hasError }"
                  />
                </div>

                <p class="help is-danger" v-if="hasError">Incorrect password</p>
              </div>

              <div class="field is-grouped">
                <div class="control">
                  <button class="button is-primary">
                    Login
                  </button>
                </div>
              </div>
            </form>
          </section>
        </section>
      </div>
    </div>
  </body>
</html>
